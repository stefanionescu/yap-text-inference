#!/usr/bin/env bash

# lint:justify -- reason: configuration file sourced by lint/security wrappers -- ticket: N/A
# shellcheck disable=SC2034

SEMGREP_IMAGE="semgrep/semgrep:1.92.0"
SEMGREP_RULES_FILE="linting/semgrep/rules.yml"
SEMGREP_TARGETS=("src" "scripts" "docker" "linting" ".githooks")
SEMGREP_EXCLUDES=("node_modules" ".venv" ".git" "tests/support/messages" "tests/support/prompts")
SEMGREP_HOOK_CONFIG="r/bash"

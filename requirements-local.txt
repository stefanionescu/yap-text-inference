#
# Lightweight dependency set for running the WebSocket test clients locally
# (e.g., macOS or CPU-only machines). Install via:
#   python3 -m venv .venv-local && source .venv-local/bin/activate
#   pip install -r requirements-local.txt
#

websockets==12.0
httpx==0.27.2
orjson==3.10.7


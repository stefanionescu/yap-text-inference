[tool.ruff]
target-version = "py310"
line-length = 120

# Exclude common build and cache directories; ruff only inspects Python files.
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "build",
    "dist",
    "scripts/build",
]

[tool.ruff.lint]
# Lint rules: pyflakes/pycodestyle, bugbear, pyupgrade, comprehensions, simplify,
# naming, security, warnings, pylint subset. Import sorting handled by isort.
select = ["E", "F", "B", "UP", "C4", "SIM", "N", "S", "W", "PL", "PLC0415"]
# Ignore docstring and annotation-style checks by default; keep formatting-related ignores
# compatible with the ruff formatter.
ignore = ["D", "ANN", "COM812", "ISC001"]

[tool.ruff.lint.per-file-ignores]
"src/state/hf.py" = ["PLC0415"]
"src/handlers/session/abort.py" = ["PLC0415"]

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["src", "tests"]
length-sort = true
length-sort-straight = true
force-sort-within-sections = true

[tool.ruff.lint.pylint]
max-args = 15
max-branches = 20
max-statements = 50

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
python_version = "3.10"
pretty = true
show_error_codes = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
strict_optional = true
ignore_missing_imports = true
disallow_untyped_defs = false
no_implicit_optional = false

[tool.isort]
profile = "black"
line_length = 120
known_first_party = ["src", "tests"]
combine_as_imports = true
length_sort = true
length_sort_straight = true
force_sort_within_sections = true

[tool.importlinter]
root_packages = ["src"]

[[tool.importlinter.contracts]]
name = "state must not import streaming"
type = "forbidden"
source_modules = ["src.state"]
forbidden_modules = ["src.streaming"]

[[tool.importlinter.contracts]]
name = "state must not depend on hf push modules"
type = "forbidden"
source_modules = ["src.state"]
forbidden_modules = ["src.hf.trt.push", "src.hf.vllm.push"]

[[tool.importlinter.contracts]]
name = "session package must not import handler instances"
type = "forbidden"
source_modules = ["src.handlers.session"]
forbidden_modules = ["src.handlers.instances"]
ignore_imports = ["src.handlers.session.abort -> src.handlers.instances"]
